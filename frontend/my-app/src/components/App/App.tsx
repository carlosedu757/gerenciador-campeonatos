import React from 'react';
import { Router } from "../Router/Router";
import './styles.css'

export const App: React.FC = () => {
  
  return (
    // <StoreProvider>
    <Router />
    // </StoreProvider>
  );
}